[project]
name = "hash-clamp"
authors = [{name = "light181", email = "danil.odinzov181@gmail.com"}]
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings"
]

[project.optional-dependencies]
dev = [
    "pytest"
]